import { Box, Button } from "@chakra-ui/react";

const App = () => {
    return (
        <Box w={'100vw'}>
            <Button colorScheme={'red'}>Login now</Button>
        </Box>
    );
}

export default App;
