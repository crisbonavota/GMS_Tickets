import { Input } from '@chakra-ui/react';

interface Props {}
const GeneralSearchFilter = (props: Props) => {
    return <Input type={'text'} placeholder={'Search'} bgColor={'white'} />;
};
export default GeneralSearchFilter;
